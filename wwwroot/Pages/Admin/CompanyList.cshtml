@page
@model CompanyListModel

@{
    ViewData["Title"] = "Start";
}

<h1>Administrere virksamheter</h1>

<form method="post">
    <table class="table">
        <caption>
            <h2>Virksamheter</h2>
        </caption>
        <thead>
            <tr>
                <th scope="col">Corporate Identity Number</th>
                <th scope="col">Name</th>
                <th scope="col">ContactPerson Name</th>
                <th scope="col">ContactPerson Email</th>
                <th scope="col">ContactPerson Phone</th>
                <th scope="col">Radera</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.CompanyList)
            {
                var contactPerson = item.ContactPersonList.FirstOrDefault();

                <tr>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.CorporateIdentityNumber
                    </td>
                    <td>
                        @contactPerson?.Name
                    </td>
                    <td>
                        @contactPerson?.Email
                    </td>
                    <td>
                        @contactPerson?.Phone
                    </td>
                    <td>
                        <button type="submit" asp-page-handler="RemoveCompany" asp-route-id="@item.Id">Radera</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</form>

<form method="post">
    <a class="button" asp-page="CompanyAdd">Opprett ny virksomhet</a>
    <button type="submit" class="button" asp-page-handler="ExcelImport">Importer virksomheter fra excel</button>
</form>
